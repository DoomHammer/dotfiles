# vim: set ft=sh sw=2 et :

# make less more friendly for non-text input files, see lesspipe(1)
command -v lesspipe.sh >/dev/null 2>&1 && eval "$(SHELL=/bin/sh lesspipe.sh)"

# enable color support of ls and also add handy aliases
if command -v dircolors >/dev/null 2>&1; then
    test -r ~/.dircolors && eval "$(dircolors -b ~/.dircolors)" || eval "$(dircolors -b)"
fi

if [ -f "$HOME/.shell-common.local" ]; then
  . "$HOME/.shell-common.local"
fi

# TODO: How to make it conditional in a nice way?
#glibcLocales=$(nix-build --no-out-link "<nixpkgs>" -A glibcLocales)
#export LOCALE_ARCHIVE="${glibcLocales}/lib/locale/locale-archive"
